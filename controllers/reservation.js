const reservationRouter = require('express').Router()
//const reservation = require('../models/reservatio')
//require('dotenv').config()


reservationRouter.post('/', (req,res)=>{

})

module.exports = reservationRouter